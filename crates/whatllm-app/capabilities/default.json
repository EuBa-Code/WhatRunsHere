{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The application window, and the controls that draw its own title bar. Nothing beyond it: the page cannot reach the network, so a model download has to go through a command into the Rust process, where the address it may use is fixed.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-is-maximized",
    "core:window:allow-close"
  ]
}
