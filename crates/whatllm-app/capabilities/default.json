{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The application window. Nothing beyond it: WhatLLM reads the machine it runs on and makes no network calls, so the webview needs no capability that could.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
